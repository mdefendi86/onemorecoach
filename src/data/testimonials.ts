import type { Testimonial } from '@/types/content'

/*
  Testimonials — Phase 0 ships empty.
  §15 open question #8: real client quotes still need to be collected.
  Components should render a "Coming soon" state or hide the section
  when testimonials.length === 0, rather than display placeholder copy.
*/
export const testimonials: Testimonial[] = []
